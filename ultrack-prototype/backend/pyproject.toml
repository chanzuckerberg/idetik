[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ultrack-learns"
version = "0.1.0"
description = "An API for HITL improvement of cell tracking data"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
]
