name: Lint, Test, and Build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        run: |
          npm ci
          npx playwright install chromium

      - name: Compile TypeScript
        run: |
          npm run compile --workspace=@idetik/core-prerelease
          npm install --workspace=@idetik/react-prerelease
          npm run compile --workspace=@idetik/react-prerelease

      - name: Lint
        run: |
          npm run lint --workspace=@idetik/core-prerelease
          npm run format-check --workspace=@idetik/core-prerelease
          npm run lint --workspace=@idetik/react-prerelease
          npm run format-check --workspace=@idetik/react-prerelease

      - name: Run tests with coverage
        run: |
          npm run test-with-coverage --workspace=@idetik/core-prerelease
          npm run test-with-coverage --workspace=@idetik/react-prerelease

  build:
    needs: lint-and-test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build:all
